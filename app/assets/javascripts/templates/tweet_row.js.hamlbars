.tweet.clearfix.thumbnail
  .profile-image
    %a{bind: {href: 'view.content.user.profileUrl'}}
      %img{bind: {src: 'view.content.user.profileImageUrl'}}
  %div
    %a{bind: {href: 'view.content.user.profileUrl'}}
      %span.full-name
        {{view.content.user.name}}
      %span.screen-name
        @{{view.content.user.screenName}}
  .text
    {{{view.linkedText}}}
